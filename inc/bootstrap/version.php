<?php

/**
 * Theme version — single source of truth.
 *
 * Update this one file to bump the version everywhere
 * (WordPress, PHPStan, PHPUnit, ThemeConfig).
 *
 * @package Nextora
 */

declare( strict_types=1 );

if ( ! defined( 'NEXTORA_VERSION' ) ) {
	define( 'NEXTORA_VERSION', '0.0.15' );
}
